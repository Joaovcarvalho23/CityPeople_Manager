# Cadastros
````
Ao clonar o repositório, vá ao terminal e execute o comando 'npm install' para instalar as dependências do node

Caso você tenha o Docker instalado, execute o comando 'docker-compose up --build' para não ter que instalar as dependências manualmente.